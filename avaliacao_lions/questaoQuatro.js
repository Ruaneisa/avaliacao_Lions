let idade = 21;
let mensagem;
if (idade >= 18) {
    mensagem = "Você pode dirigir.";
} else {
    mensagem = "Você não pode dirigir.";
}
console.log(mensagem);

//Se faz a alteração nas linhas 4 e 6, onde quando a condicional "if" for atendida exibirá no terminal a mensagem que o usuário pode dirigir.
// Caso não seja cumprida a mensagem no else que será printada no terminal.
